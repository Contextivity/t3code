# Contextivity T3 downstream

Isolated nightly distribution for the `Contextivity/t3code` fork. This directory is not part of the
generic ACP provider implementation.

Canonical runbook: [docs/operations/contextivity-downstream.md](../docs/operations/contextivity-downstream.md)

```sh
./bin/t3-ctx --help
node --test --experimental-strip-types src/*.test.ts
```
